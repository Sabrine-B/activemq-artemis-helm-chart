apiVersion: v1
kind: Service
metadata:
  name: {{ include "activemq-artemis-cm.fullname" . }}
  labels:
    {{- include "activemq-artemis-cm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "activemq-artemis-cm.selectorLabels" . | nindent 4 }}
